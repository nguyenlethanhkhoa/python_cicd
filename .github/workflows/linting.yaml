name: Python Linting Workflow
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/black_linting.yaml